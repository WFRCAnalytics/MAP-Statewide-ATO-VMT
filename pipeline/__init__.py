"""Shared library code for the ATO/VMT data pipeline.

This package is not run directly. It's imported by the numbered scripts in
`scripts/`, which are the actual entry points a user runs with `uv run`.
"""
