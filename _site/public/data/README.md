This folder is the Vite web-serving copy of processed data.

The canonical generated outputs live in `_data/processed/ato` and
`_data/processed/vmt`. The processors copy deployable files here so the browser
can fetch them while the Vite app runs.
