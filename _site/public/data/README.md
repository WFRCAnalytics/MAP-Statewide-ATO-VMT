This folder is the Vite web-serving copy of processed data.

The canonical generated outputs live in `_data/processed/ato`. The processor copies deployable files here so the browser can fetch them while the Vite app runs.
